{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "Les rappels (24h/2h/15min/parking/relances) sont déclenchés par Supabase pg_cron (toutes les 10 min via pg_net -> /api/cron/reminders), pas par Vercel. Voir supabase/pg_cron.sql."
}
